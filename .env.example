# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Password required to access the bot (users run /start <password> to authenticate)
BOT_PASSWORD=change_me

# Network: mainnet | testnet
#   mainnet → https://mainnet.opnet.org  (addresses: bc1p / bc1q / op1)
#   testnet → https://testnet.opnet.org  (addresses: tb1p / tb1q / tb1s — OPNet Signet fork)
# Override RPC only if you run a custom node:
NETWORK=testnet
# RPC_URL=https://testnet.opnet.org

# How often to poll for new blocks (milliseconds). Default: 30000 (30s)
POLL_INTERVAL_MS=30000

# Max wallets a single user can track
MAX_WALLETS_PER_USER=20

# Block explorer URL for transaction links (default: network-appropriate)
#   mainnet → https://mempool.space/tx/
#   testnet → https://mempool.opnet.org/testnet4/tx/
# MEMPOOL_URL=https://mempool.opnet.org/testnet4/tx/

# Telegram chat ID of the bot admin (enables /users and /revoke commands)
# ADMIN_CHAT_ID=123456789

# MongoDB connection URI (default: mongodb://localhost:27017/jeet-tracker)
# MONGODB_URI=mongodb://localhost:27017/jeet-tracker

# OPNet Block Indexer REST API URL (default: http://localhost:3000)
# INDEXER_URL=http://localhost:3000
